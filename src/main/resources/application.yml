spring:
    datasource:
        url: jdbc:mysql://localhost:3306/sistemaAluno
        username: root
        password: Santos!100
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialect
    thymeleaf:
        prefix: classpath:/templates/
        suffix: .html
        cache: false

